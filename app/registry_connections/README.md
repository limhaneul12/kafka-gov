# Schema Registry Connections

This module now exists only to manage Schema Registry connections used by the schema-governance slice. Kafka broker CRUD/testing was intentionally removed from the active runtime.
