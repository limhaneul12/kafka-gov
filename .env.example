# ===================================================
# Kafka Governance - Environment Configuration
# ===================================================
# 
# This is a template file. Copy this to .env and configure for your environment.
# For production deployment, see PRODUCTION_SETUP.md
#

# ===================================================
# Application Configuration
# ===================================================
APP_NAME=Kafka Governance
APP_VERSION=1.0.0
APP_DEBUG=false
APP_LOG_LEVEL=INFO

# Environment: development, staging, production
# - development: Enables Swagger docs, verbose logging
# - staging: Similar to production but with docs enabled
# - production: Disables docs, optimized for performance
APP_ENVIRONMENT=production

# ===================================================
# CORS Configuration
# ===================================================
# Comma-separated list of allowed origins
# 
# Development example:
#   APP_CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:80
# 
# Production example:
#   APP_CORS_ORIGINS=https://yourdomain.com,https://admin.yourdomain.com
# 
# Not recommended (security risk):
#   APP_CORS_ORIGINS=*
APP_CORS_ORIGINS=https://yourdomain.com

# ===================================================
# Database Configuration
# ===================================================
# MySQL connection string format:
# DATABASE_URL=mysql+asyncmy://username:password@host:port/database?charset=utf8mb4
DATABASE_URL=mysql+asyncmy://user:password@mysql:3306/kafka_gov?charset=utf8mb4
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20

# ===================================================
# Kafka Configuration
# ===================================================
# Bootstrap servers (comma-separated)
KAFKA_BOOTSTRAP_SERVERS=kafka1:9092,kafka2:9092,kafka3:9092

# Security protocol: PLAINTEXT, SSL, SASL_PLAINTEXT, SASL_SSL
KAFKA_SECURITY_PROTOCOL=PLAINTEXT

# ===================================================
# Schema Registry Configuration
# ===================================================
SCHEMA_REGISTRY_URL=http://schema-registry:8081

# ===================================================
# MinIO/Object Storage Configuration
# ===================================================
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=changeme-strong-password
MINIO_SECURE=false
MINIO_BUCKET_NAME=kafka-gov-schemas

# ===================================================
# Security Configuration
# ===================================================
# Generate a strong secret key for production!
# Example: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=changeme-use-strong-secret-key

JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# Encryption key for sensitive data (32 bytes base64 encoded)
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=changeme-generate-new-encryption-key

# ===================================================
# API Documentation Basic Auth (Optional)
# ===================================================
# Only used in non-production environments
# Uncomment these lines to enable Basic Auth for API docs
# 
# DOCS_AUTH_USER=admin
# DOCS_AUTH_PASSWORD=changeme-secure-password